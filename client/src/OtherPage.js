import React from 'react';
import { Link } from 'react-router-dom';

export default () => {
    return (
        <dev>
            Im some other page!
        <Link to="/">go back home</Link>
        </dev>
    );
};

